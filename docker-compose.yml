version: '3.8'

services:
  orchestrator:
    build: .
    container_name: aaqil_orchestrator
    ports:
      - "8000:8000"
    volumes:
      - ./.env:/app/.env
      - ./aaqil.db:/app/aaqil.db
      - ./chroma_db:/app/chroma_db
    restart: unless-stopped
